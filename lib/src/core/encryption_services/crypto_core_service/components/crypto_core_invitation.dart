/// The signed invitation a chat produced, as `Fuzz/` text.
class CryptoCoreInvitation {
  final String chatId;
  final String content;

  const CryptoCoreInvitation({
    required this.chatId,
    required this.content,
  });
}
