/// The signed acceptance a chat produced, as `Fuzz/` text.
class CryptoCoreAcceptance {
  final String chatId;
  final String content;

  const CryptoCoreAcceptance({
    required this.chatId,
    required this.content,
  });
}
