# Test vectors for `PROTOCOL.md`

Machine-generated by `rust/fuzzy_crypto_core/src/vectors.rs` and checked on every `cargo test`
(`vectors::committed_vectors_match`). Do not edit by hand: after a deliberate format change run

```
FUZZY_WRITE_VECTORS=1 cargo test --locked vectors::write_vectors     # from rust/fuzzy_crypto_core
```

which rewrites every file and asserts that a second generation is byte-identical.

## Files

- `<name>.hex` — the bytes, lowercase hex, 32 bytes per line. `xxd -r -p <name>.hex | xxd` shows them with offsets.
- `<name>.txt` — for wire blobs, the paste-able `Fuzz/` + base64url form (what a user would copy).
- `<name>.json` — every input the vector was built from (keys, seeds, salts, nonces, chat id, text) and the
  AAD/KDF rule that applies. Secrets in these files are public test constants (`01×32`, `pw`, …), never
  production material.
- `state_file.body.json` — the decrypted body of `state_file.hex`.

## Common inputs

| Input | Value |
|---|---|
| chat id | `6f1e9b2c-3d4a-4f5b-8c6d-7e8f9a0b1c2d` |
| A's account | Ed25519 seed `01×32`, Curve25519 secret `02×32`, one one-time-key secret `03×32` (rebuilt through vodozemac's `AccountPickle`) |
| B's account | Ed25519 seed `04×32`, Curve25519 secret `05×32`, no one-time key |
| password | `pw` |
| Argon2id | `m = 65536` KiB, `t = 4`, `p = 1`, 32-byte output, version 0x13 |
| salt / nonce (blobs) | `11×16` / `22×24` |
| store key = vault master key | `33×32` |
| local-seal nonce | `44×24` |
| file key (fixed-key container), file-key body | `42×32` |
| file nonce prefix / file salt | `90 91 … a2` (19 bytes) / `a0 a1 … af` (16 bytes) |

## The vectors

| Vector | What it pins | Bytes | Independent derivation it was checked against |
|---|---|---|---|
| `invitation` | 0x01 layout, A's derived public keys, the signed range (envelope included), Ed25519 = the standard one | 203 | F2-3 reviewer: python `cryptography` 50 and `pynacl` 1.6 from the same three seeds (`audit_golden_f23.py`) — byte-identical |
| `acceptance` | 0x02 layout with a 5-byte placeholder pre-key body (`a1a2a3a4a5`; a real body is randomised), B's derived keys, real signature | 178 | F5-1: python `cryptography` Ed25519 over the table-built bytes — byte-identical |
| `message` | 0x03 layout (`olm_type` Normal, placeholder body `deadbeef`) and its text form | 11 | F2-1 reviewer (`golden_check.py`, from the plan tables) |
| `inner_header_handshake` | the 116-byte handshake plaintext B encrypts: B→A, counter 0, content type 0x00, empty body | 116 | F5-1: python, from PROTOCOL §7.1 |
| `inner_header_text` | A→B, counter 7, content type 0x01, body `hello` | 121 | F5-1: python |
| `file_key_body` | content-type-0x02 body: key `42×32` ‖ `000a` ‖ `report.pdf` | 44 | F3-2 developer/reviewer golden; F5-1: python |
| `inner_header_file_key` | A→B, counter 8, content type 0x02, body = `file_key_body` | 160 | F5-1: python |
| `argon2id_kek` | Argon2id(`pw`, `11×16`, production parameters) | 32 | F5-1: `argon2-cffi` 25.1 `hash_secret_raw(type=ID, version=19)` |
| `wrapped_store_key` | 0x10, AAD `store-key`: header ‖ ct ‖ tag `0343f644…` | 103 | F2-2 reviewer (`audit_golden_f22.py`: argon2-cffi + pycryptodome 3.23) — byte-identical; negative controls (AAD `store-key\0`, t=3, p=4) differ |
| `wrapped_vault_key` | 0x10, AAD `vault-key`: same header and ciphertext, tag `47a1848d…` | 103 | F4-1 reviewer's primitives (`review_golden_f41.py`) recomputed for the `fd0850e` AAD change — byte-identical |
| `local_key` | HKDF-SHA256(store key, no salt, `fuzzy-local-seal-v1`) | 32 | F5-1: pycryptodome `HKDF` |
| `local_seal` | 0x20 of `hello` under the local key, AAD `local-seal` | 51 | F2-2 reviewer — byte-identical |
| `vault_item` | 0x20 of `item` under the master key directly, AAD `vault-item` | 50 | F4-1 reviewer — byte-identical |
| `password_sealed_text` | 0x05 of `hello`, AAD = its own first 31 bytes; plus the text form | 76 | F4-1 reviewer (`review_golden_f41.py`) — byte-identical; AAD readings B–E (with nonce / without salt / envelope only / none) all differ |
| `state_file` | the sealed state file of A's freshly invited chat: AAD `chat-state` ‖ chat id, key = store key, body = `state_file.body.json` | 1550 | F5-1: pycryptodome over the committed body |
| `file_header_password` | 0x04 header, key mode 0x02, 1 MiB chunk size | 55 | F2-1 reviewer; F3-1 `golden_header` |
| `file_header_chat` | 0x04 header, key mode 0x01, placeholder Olm body `f1f2f3` | 36 | F2-1 reviewer |
| `file_container_password` | one-chunk container (43-byte plaintext) with a real Argon2id file key; nonce `prefix ‖ 00000000 ‖ 01`, AAD `header ‖ 00000000` | 114 | F5-1: argon2-cffi + pycryptodome from PROTOCOL §9.1 |
| `file_container_fixed_key` | two-chunk container (65 536 + 5 bytes of the xorshift pattern) under key `42×32`: chunk 0 `… ‖ 00000000 ‖ 00`, chunk 1 `… ‖ 00000001 ‖ 01` | 65 628 | F3-1 reviewer (`review_golden_f31.py`, pycryptodome): chunk-0 first 16 bytes `d3cd63e6…`, chunk-0 tag `c64860d9…`, chunk 1 `49fa5b09…` — identical; the pycryptodome-built file also opens in Rust |
| `safety_number` | `SHA-512(domain ‖ 0 ‖ chat id ‖ 0 ‖ min ‖ max)` → 12 five-digit groups, keys `00…1f` / `ff…e0` | 71 chars | F2-5 developer and reviewer, stdlib `hashlib` from the plan text only — identical, order-independent |

"F<n>-<m> reviewer" refers to the independent review of that feature (recorded in the project's build log);
"F5-1" refers to the derivation done while writing `PROTOCOL.md`. In every case the Python was written from
the specification tables, not from the Rust.

## What is not here, and why

vodozemac draws randomness (base key, ratchet key) inside `create_outbound_session` and `encrypt` with no
injection point, so no vector contains a real Olm message: the acceptance, the message and the chat-mode
file header carry placeholders where the Olm body goes. The Olm message encoding is vodozemac's own
(audited) format and is outside `PROTOCOL.md`'s scope; the layouts *around* it — the length prefixes, the
signed range, the header bytes that become every chunk's AAD — are what these vectors pin.
